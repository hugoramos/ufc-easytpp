# HoTHP Presentation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create a 37-slide Beamer presentation in Portuguese about HoTHP for a 45–60 minute graduate seminar, following a progressive narrative arc from motivation to experimental results.

**Architecture:** Single `apresentacao.tex` file using the Beamer Madrid theme, with all figures copied into a local `figures/` directory. New figures are generated by Python scripts in `figures_src/`. A break-even experiment script lives in `notebooks/`.

**Tech Stack:** LaTeX (Beamer, booktabs, amsmath), Python 3.9 (matplotlib, numpy) for figure generation, easy_tpp runner for the break-even experiment.

---

## File Structure

```
overleaf-apresentacao/
  apresentacao.tex          # main presentation file (all 37 slides)
  refs.bib                  # bibliography (copy of overleaf-bracis/refs.bib)
  figures/
    hawkes_illustration.pdf   # copy from overleaf-bracis/figures/
    rope_vs_hope.pdf
    architecture.pdf
    attention_profile.pdf
    main_result.pdf
    kernel_alignment.pdf
    attention_comparison.pdf  # new — generated by figures_src/gen_attention_comparison.py
    cost_curve.pdf            # new — generated by figures_src/gen_cost_curve.py
  figures_src/
    gen_attention_comparison.py
    gen_cost_curve.py
notebooks/
  breakeven_experiment.py     # new — measures wall-clock training time vs sequence length
```

---

## Task 1: Set up project directory and skeleton

**Files:**
- Create: `overleaf-apresentacao/apresentacao.tex`
- Create: `overleaf-apresentacao/refs.bib`
- Create: `overleaf-apresentacao/figures/` (directory with copied figures)

- [ ] **Step 1: Create directories**

```bash
mkdir -p overleaf-apresentacao/figures
mkdir -p overleaf-apresentacao/figures_src
```

- [ ] **Step 2: Copy bibliography and existing figures**

```bash
cp overleaf-bracis/refs.bib overleaf-apresentacao/refs.bib
cp overleaf-bracis/figures/hawkes_illustration.pdf overleaf-apresentacao/figures/
cp overleaf-bracis/figures/rope_vs_hope.pdf overleaf-apresentacao/figures/
cp overleaf-bracis/figures/architecture.pdf overleaf-apresentacao/figures/
cp overleaf-bracis/figures/attention_profile.pdf overleaf-apresentacao/figures/
cp overleaf-bracis/figures/main_result.pdf overleaf-apresentacao/figures/
cp overleaf-bracis/figures/kernel_alignment.pdf overleaf-apresentacao/figures/
```

- [ ] **Step 3: Create skeleton `apresentacao.tex` that compiles without errors**

```latex
\documentclass[aspectratio=169]{beamer}
\usetheme{Madrid}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[portuguese]{babel}
\usepackage{amsmath,amssymb,bm}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{subcaption}

\graphicspath{{figures/}}

\title{HoTHP: Processo de Hawkes com Codificação de Posição Rotacional Hiperbólica}
\subtitle{Extrapolação de Comprimento em Processos Temporais de Ponto}
\author{Hugo Ramos Soares}
\institute{Universidade Federal do Ceará \\ Orientador: César Lincoln C. Mattos}
\date{2026}

\begin{document}

\begin{frame}
  \titlepage
\end{frame}

\begin{frame}{Sumário}
  \tableofcontents
\end{frame}

\end{document}
```

- [ ] **Step 4: Compile and verify it produces a PDF without errors**

Run from inside `overleaf-apresentacao/`:
```bash
cd overleaf-apresentacao && pdflatex apresentacao.tex
```
Expected: `apresentacao.pdf` with 2 pages (title + table of contents), no errors.

- [ ] **Step 5: Commit**

```bash
git add overleaf-apresentacao/
git commit -m "presentation: scaffold Beamer project with Madrid theme"
```

---

## Task 2: Generate new figure — attention comparison (RoTHP vs HoTHP)

**Files:**
- Create: `overleaf-apresentacao/figures_src/gen_attention_comparison.py`
- Create: `overleaf-apresentacao/figures/attention_comparison.pdf`

This figure shows, side by side, the attention weight matrix of RoTHP and HoTHP on a sequence of 40 events. RoTHP shows no recency bias; HoTHP shows exponential decay from the diagonal.

- [ ] **Step 1: Create the generation script**

```python
# overleaf-apresentacao/figures_src/gen_attention_comparison.py
"""Side-by-side attention weight matrices for RoTHP and HoTHP.
Shows that RoTHP has no recency bias; HoTHP decays from the diagonal."""
import matplotlib
matplotlib.use('Agg')
import matplotlib.pyplot as plt
import numpy as np
import os

np.random.seed(0)
L = 40
d = 64
half_d = d // 2

j = np.arange(1, half_d + 1)
theta_j = 10000.0 ** (-2.0 * (j - 1) / d)
theta_prime = np.max(theta_j) + 0.05

# Synthetic timestamps: irregular gaps drawn from exponential
gaps = np.random.exponential(scale=1.0, size=L)
times = np.cumsum(gaps)  # shape [L]

# Random unit queries and keys (same for both models)
Q = np.random.randn(L, d)
K = np.random.randn(L, d)
Q /= np.linalg.norm(Q, axis=-1, keepdims=True)
K /= np.linalg.norm(K, axis=-1, keepdims=True)

Q1, Q2 = Q[:, 0::2], Q[:, 1::2]  # [L, half_d]
K1, K2 = K[:, 0::2], K[:, 1::2]

# RoTHP scores: q_rot dot k_rot using cos/sin kernel
scores_rothp = np.zeros((L, L))
for i in range(L):
    cos_i = np.cos(times[i] * theta_j)
    sin_i = np.sin(times[i] * theta_j)
    q1r = Q1[i] * cos_i - Q2[i] * sin_i
    q2r = Q1[i] * sin_i + Q2[i] * cos_i
    for jj in range(L):
        cos_j = np.cos(times[jj] * theta_j)
        sin_j = np.sin(times[jj] * theta_j)
        k1r = K1[jj] * cos_j - K2[jj] * sin_j
        k2r = K1[jj] * sin_j + K2[jj] * cos_j
        scores_rothp[i, jj] = (np.sum(q1r * k1r) + np.sum(q2r * k2r)) / np.sqrt(d)

# HoTHP scores: hyperbolic kernel per pair
scores_hothp = np.zeros((L, L))
for i in range(L):
    for jj in range(L):
        delta = times[i] - times[jj]
        abs_d = abs(delta)
        sign_d = np.sign(delta) if delta != 0 else 0.0
        exp_minus = np.exp(-abs_d * (theta_prime - theta_j))
        exp_plus  = np.exp(-abs_d * (theta_prime + theta_j))
        dc = (exp_minus + exp_plus) / 2.0
        ds = sign_d * (exp_minus - exp_plus) / 2.0
        scores_hothp[i, jj] = (
            np.sum((Q1[i]*K1[jj] + Q2[i]*K2[jj]) * dc +
                   (Q1[i]*K2[jj] + Q2[i]*K1[jj]) * ds)
        ) / np.sqrt(d)

# Apply causal mask and softmax
def causal_softmax(scores):
    mask = np.triu(np.ones_like(scores), k=1) * -1e9
    s = scores + mask
    s = s - s.max(axis=-1, keepdims=True)
    e = np.exp(s)
    return e / e.sum(axis=-1, keepdims=True)

w_rothp = causal_softmax(scores_rothp)
w_hothp = causal_softmax(scores_hothp)

fig, axes = plt.subplots(1, 2, figsize=(10, 4.5))

for ax, w, title in zip(axes,
                         [w_rothp, w_hothp],
                         ['RoTHP', 'HoTHP']):
    im = ax.imshow(w, aspect='auto', cmap='Blues', vmin=0)
    ax.set_title(title, fontsize=13)
    ax.set_xlabel('Key event index', fontsize=10)
    ax.set_ylabel('Query event index', fontsize=10)
    plt.colorbar(im, ax=ax, fraction=0.046, pad=0.04)

plt.tight_layout()
out_path = os.path.join(os.path.dirname(__file__), '..', 'figures', 'attention_comparison.pdf')
fig.savefig(out_path, dpi=300, bbox_inches='tight')
plt.close(fig)
print(f"Saved {out_path}")
```

- [ ] **Step 2: Run the script**

```bash
python3.9 overleaf-apresentacao/figures_src/gen_attention_comparison.py
```
Expected: `overleaf-apresentacao/figures/attention_comparison.pdf` created. RoTHP panel should show attention spread across the row; HoTHP panel should show concentration near the diagonal.

- [ ] **Step 3: Commit**

```bash
git add overleaf-apresentacao/figures_src/gen_attention_comparison.py overleaf-apresentacao/figures/attention_comparison.pdf
git commit -m "presentation: add attention comparison figure (RoTHP vs HoTHP)"
```

---

## Task 3: Generate new figure — training cost curve

**Files:**
- Create: `overleaf-apresentacao/figures_src/gen_cost_curve.py`
- Create: `overleaf-apresentacao/figures/cost_curve.pdf`

Shows the $O(L^2)$ scaling of attention cost versus sequence length, motivating why training on long sequences is expensive.

- [ ] **Step 1: Create the generation script**

```python
# overleaf-apresentacao/figures_src/gen_cost_curve.py
"""Training cost curve illustrating O(L^2) scaling of attention."""
import matplotlib
matplotlib.use('Agg')
import matplotlib.pyplot as plt
import numpy as np
import os

L = np.array([128, 256, 512, 1024, 2048])
cost = (L / 128.0) ** 2  # normalized so L=128 => cost=1

fig, ax = plt.subplots(figsize=(6, 4))
ax.plot(L, cost, 'o-', color='#1565C0', linewidth=2, markersize=7)

# Annotate each point
for l, c in zip(L, cost):
    ax.annotate(f'{c:.0f}x', xy=(l, c), xytext=(8, 4),
                textcoords='offset points', fontsize=9, color='#1565C0')

ax.set_xlabel('Sequence length $L$', fontsize=12)
ax.set_ylabel('Relative training cost', fontsize=12)
ax.set_title('Attention cost grows as $O(L^2)$', fontsize=12)
ax.spines['top'].set_visible(False)
ax.spines['right'].set_visible(False)
ax.tick_params(labelsize=10)
ax.set_xticks(L)
ax.set_xticklabels([str(l) for l in L])

plt.tight_layout()
out_path = os.path.join(os.path.dirname(__file__), '..', 'figures', 'cost_curve.pdf')
fig.savefig(out_path, dpi=300, bbox_inches='tight')
plt.close(fig)
print(f"Saved {out_path}")
```

- [ ] **Step 2: Run the script**

```bash
python3.9 overleaf-apresentacao/figures_src/gen_cost_curve.py
```
Expected: `overleaf-apresentacao/figures/cost_curve.pdf` created.

- [ ] **Step 3: Commit**

```bash
git add overleaf-apresentacao/figures_src/gen_cost_curve.py overleaf-apresentacao/figures/cost_curve.pdf
git commit -m "presentation: add O(L^2) training cost curve figure"
```

---

## Task 4: Block 1 — Motivação (slides 1–4)

**Files:**
- Modify: `overleaf-apresentacao/apresentacao.tex`

Add `\section{Motivação}` and the 4 motivation slides. Slide 1 is the title page (already handled by `\titlepage`). Replace the skeleton body with the content below and keep adding to it in subsequent tasks.

- [ ] **Step 1: Replace the body of `apresentacao.tex` with Block 1 content**

Replace everything between `\begin{document}` and `\end{document}` with:

```latex
\begin{document}

% ── Slide 1: título ─────────────────────────────────────────────────────────
\begin{frame}
  \titlepage
\end{frame}

% ── Sumário ─────────────────────────────────────────────────────────────────
\begin{frame}{Sumário}
  \tableofcontents
\end{frame}

% ============================================================================
\section{Motivação}
% ============================================================================

% ── Slide 2: sequências de eventos no mundo real ─────────────────────────────
\begin{frame}{Sequências de eventos no mundo real}
  Fenômenos que ocorrem em tempo contínuo e que precisamos modelar:
  \vspace{0.4em}
  \begin{columns}[T]
    \begin{column}{0.25\textwidth}
      \centering
      \textbf{Telemetria} \\
      \small falhas em sistemas, alertas de infraestrutura
    \end{column}
    \begin{column}{0.25\textwidth}
      \centering
      \textbf{Redes sociais} \\
      \small retweets, curtidas, respostas em cascata
    \end{column}
    \begin{column}{0.25\textwidth}
      \centering
      \textbf{Saúde} \\
      \small internações, exames, prescrições de um paciente
    \end{column}
    \begin{column}{0.25\textwidth}
      \centering
      \textbf{Manutenção} \\
      \small falhas em equipamentos industriais
    \end{column}
  \end{columns}
  \vspace{0.8em}
  \begin{block}{O que há em comum?}
    Cada domínio gera uma sequência ordenada de eventos com timestamps:
    $\mathcal{S} = \{(t_1, k_1), (t_2, k_2), \ldots, (t_N, k_N)\}$
  \end{block}
\end{frame}

% ── Slide 3: o que queremos modelar? ────────────────────────────────────────
\begin{frame}{O que queremos modelar?}
  Dada uma sequência de eventos passados, queremos responder:
  \begin{itemize}
    \item \textbf{Quando} ocorrerá o próximo evento?
    \item \textbf{De que tipo} será esse evento?
  \end{itemize}
  \vspace{0.5em}
  A ferramenta central é a \textbf{intensidade condicional}:
  \[
    \lambda(t \mid \mathcal{H}_t) = \lim_{dt \to 0}
      \frac{P(\text{evento em } [t, t+dt) \mid \mathcal{H}_t)}{dt}
  \]
  onde $\mathcal{H}_t$ representa todos os eventos anteriores ao instante $t$.
  \vspace{0.5em}
  \begin{block}{Objetivo do aprendizado}
    Aprender $\lambda(t)$ a partir de dados, de forma que o modelo generalize
    para sequências com comprimentos diferentes dos vistos no treinamento.
  \end{block}
\end{frame}

% ── Slide 4: o problema prático ──────────────────────────────────────────────
\begin{frame}{O problema prático}
  \begin{columns}[T]
    \begin{column}{0.55\textwidth}
      A atenção do Transformer tem custo $O(L^2)$ em memória e tempo.
      \vspace{0.4em}
      \begin{itemize}
        \item Dobrar o comprimento da sequência quadruplica o custo.
        \item Empresas treinam em sequências curtas por restrição de recursos.
        \item Em produção, as sequências podem ser muito mais longas.
      \end{itemize}
      \vspace{0.4em}
      \textbf{Pergunta central:} é possível treinar em sequências curtas e
      obter bom desempenho em sequências longas?
    \end{column}
    \begin{column}{0.42\textwidth}
      \includegraphics[width=\textwidth]{cost_curve}
    \end{column}
  \end{columns}
\end{frame}
```

- [ ] **Step 2: Compile and verify**

```bash
cd overleaf-apresentacao && pdflatex apresentacao.tex
```
Expected: PDF with 5 pages (title + TOC + 3 slides), no errors.

- [ ] **Step 3: Commit**

```bash
git add overleaf-apresentacao/apresentacao.tex
git commit -m "presentation: add Block 1 (Motivacao)"
```

---

## Task 5: Block 2 — Processos Temporais de Ponto e o Processo de Hawkes (slides 5–9)

**Files:**
- Modify: `overleaf-apresentacao/apresentacao.tex`

Append after the last frame of Block 1.

- [ ] **Step 1: Add Block 2 content**

Append before `\end{document}`:

```latex
% ============================================================================
\section{Processos Temporais de Ponto}
% ============================================================================

% ── Slide 5: o que é um PTT? ─────────────────────────────────────────────────
\begin{frame}{Processos Temporais de Ponto (PTT)}
  Um PTT é um modelo estocástico que descreve sequências de eventos em
  tempo contínuo.
  \begin{itemize}
    \item Sequência multivariada: $\mathcal{S} = \{(t_i, k_i)\}_{i=1}^N$,
          onde $k_i$ é o tipo do evento $i$ e $t_i$ é o instante.
    \item A intensidade condicional $\lambda_k(t \mid \mathcal{H}_t)$
          captura a taxa instantânea do tipo $k$ dado o histórico.
    \item Quanto maior $\lambda(t)$, maior a probabilidade de um novo evento
          ocorrer em $[t, t+dt)$.
  \end{itemize}
\end{frame}

% ── Slide 6: o processo de Hawkes ────────────────────────────────────────────
\begin{frame}{O Processo de Hawkes}
  O Processo de Hawkes \cite{hawkes1971spectra} é um PTT com
  \textbf{auto-excitação}: eventos passados aumentam a probabilidade de
  novos eventos.
  \[
    \lambda(t) = \mu + \sum_{t_i < t} \varphi(t - t_i)
  \]
  \begin{itemize}
    \item $\mu$: taxa de base (intensidade estacionária).
    \item $\varphi(t - t_i)$: função de excitação, tipicamente exponencial:
          $\varphi(\tau) = \alpha e^{-\beta \tau}$.
    \item Cada evento causa um salto em $\lambda$ que decai com taxa $\beta$.
  \end{itemize}
  \begin{block}{Analogia intuitiva}
    Terremotos: um abalo principal aumenta a probabilidade de réplicas,
    que por sua vez podem gerar novas réplicas.
  \end{block}
\end{frame}

% ── Slide 7: ilustração do processo de Hawkes ────────────────────────────────
\begin{frame}{Ilustração do Processo de Hawkes}
  \begin{figure}
    \centering
    \includegraphics[width=0.88\textwidth]{hawkes_illustration}
    \caption{Processo de Hawkes multivariado com dois tipos de eventos.
             Cada evento causa um salto em $\lambda(t)$, que decai
             exponencialmente.}
  \end{figure}
\end{frame}

% ── Slide 8: como treinar? ───────────────────────────────────────────────────
\begin{frame}{Como treinar esses modelos?}
  O treinamento maximiza a verossimilhança dos dados observados.
  A função de custo é a \textbf{log-verossimilhança negativa} (NLL):
  \[
    \mathcal{L} = -\left[
      \sum_{i=1}^{N} \log \lambda(t_i)
      - \int_0^T \lambda(t) \, dt
    \right]
  \]
  \begin{itemize}
    \item Primeiro termo: penaliza modelos que atribuem baixa intensidade
          nos instantes em que eventos de fato ocorreram.
    \item Segundo termo: penaliza modelos que preveem muitos eventos em
          momentos em que não ocorreu nada.
  \end{itemize}
  Quanto menor o NLL, melhor o modelo.
\end{frame}

% ── Slide 9: comparativo de abordagens ──────────────────────────────────────
\begin{frame}{Abordagens para modelar PTTs}
  \begin{table}
    \centering
    \begin{tabular}{llll}
      \toprule
      \textbf{Família} & \textbf{Exemplo} & \textbf{Vantagem} & \textbf{Limitação} \\
      \midrule
      Paramétrica & Hawkes exp. & Interpretável, rápido & Forma funcional fixa \\
      Baseada em LSTM & NHP & Flexível, capta dep. & Treinamento lento \\
      Baseada em Transformer & THP & Paralelizável & Custo $O(L^2)$ \\
      \bottomrule
    \end{tabular}
    \caption{Comparativo entre famílias de modelos para PTTs.}
  \end{table}
\end{frame}
```

- [ ] **Step 2: Compile and verify**

```bash
cd overleaf-apresentacao && pdflatex apresentacao.tex && pdflatex apresentacao.tex
```
Run twice so that `\tableofcontents` picks up the new section. Expected: PDF growing, no errors.

- [ ] **Step 3: Commit**

```bash
git add overleaf-apresentacao/apresentacao.tex
git commit -m "presentation: add Block 2 (TPP e Processo de Hawkes)"
```

---

## Task 6: Block 3 — Modelos Neurais: do LSTM ao Transformer (slides 10–13)

**Files:**
- Modify: `overleaf-apresentacao/apresentacao.tex`

- [ ] **Step 1: Append Block 3 before `\end{document}`**

```latex
% ============================================================================
\section{Modelos Neurais para PTTs}
% ============================================================================

% ── Slide 10: NHP ────────────────────────────────────────────────────────────
\begin{frame}{Neural Hawkes Process (NHP)}
  Mei e Eisner \cite{mei2017neural} propuseram usar uma LSTM para modelar
  a intensidade de um PTT.
  \begin{itemize}
    \item A LSTM mantém um estado oculto $\mathbf{h}_i$ que resume os
          eventos anteriores.
    \item A intensidade é calculada a partir de $\mathbf{h}_i$ com uma
          função de ativação apropriada.
    \item Captura dependências complexas entre eventos de tipos diferentes.
  \end{itemize}
  \begin{block}{Limitações}
    Treinamento sequencial (não paralelizável), gradientes instáveis em
    sequências longas, tempo de inferência cresce com o comprimento.
  \end{block}
\end{frame}

% ── Slide 11: Transformers ───────────────────────────────────────────────────
\begin{frame}{Transformers \cite{vaswani2017attention}}
  O mecanismo de atenção permite que cada evento ``consulte'' todos os
  eventos anteriores simultaneamente:
  \[
    \text{Atenção}(Q, K, V) = \text{softmax}\!\left(
      \frac{QK^\top}{\sqrt{d_k}}
    \right) V
  \]
  \begin{itemize}
    \item \textbf{Paralelizável:} todos os pares são calculados de uma vez.
    \item \textbf{Atenção multi-cabeça:} diferentes subconjuntos de
          dimensões capturam diferentes tipos de relação.
    \item Revolucionou o processamento de linguagem natural.
  \end{itemize}
\end{frame}

% ── Slide 12: THP ────────────────────────────────────────────────────────────
\begin{frame}{Transformer Hawkes Process (THP)}
  Zuo et al. \cite{zuo2020transformer} adaptaram o Transformer para PTTs.
  \begin{itemize}
    \item Cada evento é representado por um embedding de tipo somado a um
          encoding temporal sinusoidal.
    \item O mecanismo de atenção causal agrega informações de eventos
          passados.
    \item Supera modelos baseados em LSTM em múltiplos benchmarks.
  \end{itemize}
  \begin{alertblock}{Limitação}
    O encoding temporal usa timestamps absolutos. O mesmo conjunto de
    eventos, deslocado no tempo, produz representações diferentes.
    O modelo \textbf{não é invariante temporal}.
  \end{alertblock}
\end{frame}

% ── Slide 13: limitação do THP ──────────────────────────────────────────────
\begin{frame}{Por que invariância temporal importa?}
  \begin{columns}[T]
    \begin{column}{0.55\textwidth}
      Um bom modelo de PTT deve depender apenas das
      \textbf{diferenças de tempo} entre eventos, não dos timestamps
      absolutos.
      \vspace{0.4em}
      \begin{itemize}
        \item O padrão de excitação de um Processo de Hawkes depende de
              $\Delta t = t_i - t_j$, não de $t_i$ ou $t_j$ isoladamente.
        \item Modelos com encoding absoluto precisam reaprender o padrão
              a cada faixa de tempo.
        \item Isso limita a capacidade de generalização fora do domínio
              de treinamento.
      \end{itemize}
    \end{column}
    \begin{column}{0.42\textwidth}
      \begin{block}{Pergunta}
        Como incorporar informação temporal de forma \textbf{relativa} ao
        mecanismo de atenção?
      \end{block}
    \end{column}
  \end{columns}
\end{frame}
```

- [ ] **Step 2: Compile and verify**

```bash
cd overleaf-apresentacao && pdflatex apresentacao.tex
```
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add overleaf-apresentacao/apresentacao.tex
git commit -m "presentation: add Block 3 (NHP, Transformers, THP)"
```

---

## Task 7: Block 4 — Extrapolação de Comprimento (slides 14–17)

**Files:**
- Modify: `overleaf-apresentacao/apresentacao.tex`

- [ ] **Step 1: Append Block 4 before `\end{document}`**

```latex
% ============================================================================
\section{Extrapolação de Comprimento}
% ============================================================================

% ── Slide 14: o problema em LLMs ────────────────────────────────────────────
\begin{frame}{O problema em modelos de linguagem}
  Press et al. \cite{press2022train} identificaram que modelos de linguagem
  treinados em sequências de comprimento $L$ degradam quando testados em
  sequências de comprimento $> L$.
  \begin{itemize}
    \item A perplexidade cresce de forma acentuada fora do domínio de
          treinamento.
    \item Propostas como ALiBi e RoPE surgiram como respostas, adicionando
          um viés posicional relativo ao mecanismo de atenção.
    \item A ideia central: treinar em sequências curtas e \textbf{testar em
          sequências longas} (\emph{train short, test long}).
  \end{itemize}
\end{frame}

% ── Slide 15: custo de treinamento ──────────────────────────────────────────
\begin{frame}{Por que não treinar em sequências longas?}
  \begin{columns}[T]
    \begin{column}{0.52\textwidth}
      O custo da atenção cresce com o \textbf{quadrado} do comprimento:
      \vspace{0.4em}
      \begin{itemize}
        \item Dobrar $L$ quadruplica o custo.
        \item Uma empresa que precisasse treinar em $L = 2048$ pagaria
              $256\times$ o custo de treinar em $L = 128$.
        \item Extrapolação bem-sucedida permite treinar barato e implantar
              em sequências longas.
      \end{itemize}
    \end{column}
    \begin{column}{0.45\textwidth}
      \includegraphics[width=\textwidth]{cost_curve}
    \end{column}
  \end{columns}
\end{frame}

% ── Slide 16: o mesmo problema em PTTs ──────────────────────────────────────
\begin{frame}{O mesmo problema em PTTs}
  Em Processos Temporais de Ponto, a degradação tem uma forma precisa:
  \begin{itemize}
    \item O NLL aumenta quando o modelo é testado em sequências com mais
          eventos do que as vistas no treinamento.
    \item Modelos com encoding absoluto (THP) falham por construção fora
          do domínio temporal de treinamento.
    \item Modelos com encoding relativo (RoTHP) melhoram, mas ainda não
          capturam o decaimento característico do Processo de Hawkes.
  \end{itemize}
  \begin{alertblock}{Lacuna na literatura}
    Até o RoTHP, não havia um modelo de PTT com extrapolação estável
    e alinhado com a estrutura do Processo de Hawkes.
  \end{alertblock}
\end{frame}

% ── Slide 17: definição formal ──────────────────────────────────────────────
\begin{frame}{Protocolo \emph{Train Short, Test Long}}
  \begin{block}{Definição}
    O modelo é treinado em sequências com $N_{\text{train}}$ eventos e
    avaliado em sequências com $\alpha \cdot N_{\text{train}}$ eventos,
    onde $\alpha > 1$ é o \textbf{fator de extrapolação}.
  \end{block}
  \vspace{0.5em}
  \begin{itemize}
    \item Avaliamos $\alpha \in \{1, 2, 5, 10\}$.
    \item A métrica é o NLL médio sobre sequências de teste.
    \item Um modelo que extrapola bem deve manter NLL estável
          para valores crescentes de $\alpha$.
  \end{itemize}
\end{frame}
```

- [ ] **Step 2: Compile and verify**

```bash
cd overleaf-apresentacao && pdflatex apresentacao.tex
```
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add overleaf-apresentacao/apresentacao.tex
git commit -m "presentation: add Block 4 (Extrapolacao de Comprimento)"
```

---

## Task 8: Block 5 — RoPE e RoTHP (slides 18–21)

**Files:**
- Modify: `overleaf-apresentacao/apresentacao.tex`

- [ ] **Step 1: Append Block 5 before `\end{document}`**

```latex
% ============================================================================
\section{RoPE e RoTHP}
% ============================================================================

% ── Slide 18: RoPE ───────────────────────────────────────────────────────────
\begin{frame}{Rotary Position Embedding (RoPE)}
  Su et al. \cite{su2024roformer} propuseram codificar a posição relativa
  \textbf{rotacionando} os vetores de query e key antes do produto interno:
  \[
    \text{score}(i, j) = (R_{\theta_i} \mathbf{q})^\top (R_{\theta_j} \mathbf{k})
  \]
  \begin{itemize}
    \item $R_\theta$ é uma matriz de rotação $2 \times 2$ com frequência $\theta$.
    \item O produto interno depende apenas da \textbf{diferença} de posição
          $\theta_i - \theta_j$, garantindo invariância relativa.
    \item Custo adicional: $O(L \cdot d)$, pois cada vetor é rotacionado
          individualmente antes da atenção.
  \end{itemize}
\end{frame}

% ── Slide 19: RoTHP ──────────────────────────────────────────────────────────
\begin{frame}{RoTHP: RoPE para PTTs}
  Dai et al. \cite{dai2026rothp} adaptaram o RoPE para sequências de
  eventos temporais, substituindo a posição discreta por $\Delta t$:
  \begin{itemize}
    \item Os vetores $\mathbf{q}$ e $\mathbf{k}$ são rotacionados com
          ângulo proporcional ao timestamp de cada evento.
    \item O produto interno captura a diferença temporal
          $\Delta t = t_i - t_j$ de forma relativa.
    \item O modelo passa a ser \textbf{invariante temporal}: transladar
          todos os timestamps não altera os scores de atenção.
  \end{itemize}
  \begin{block}{Avanço em relação ao THP}
    RoTHP remove a dependência de timestamps absolutos, o que é fundamental
    para generalização fora do domínio de treinamento.
  \end{block}
\end{frame}

% ── Slide 20: limitação do RoTHP ─────────────────────────────────────────────
\begin{frame}{Limitação do RoTHP}
  \begin{columns}[T]
    \begin{column}{0.52\textwidth}
      O kernel trigonométrico do RoPE é oscilatório e \textbf{não decai}
      com $|\Delta t|$.
      \vspace{0.4em}
      \begin{itemize}
        \item Eventos distantes no tempo recebem atenção semelhante a
              eventos recentes.
        \item Isso contradiz a estrutura do Processo de Hawkes, onde a
              influência de um evento \textbf{decai exponencialmente}.
        \item O modelo não captura o viés de recência que é intrínseco
              à auto-excitação.
      \end{itemize}
    \end{column}
    \begin{column}{0.45\textwidth}
      \includegraphics[width=\textwidth]{rope_vs_hope}
    \end{column}
  \end{columns}
\end{frame}

% ── Slide 21: comparativo de matrizes de atenção ────────────────────────────
\begin{frame}{Perfil de atenção: RoTHP vs HoTHP}
  \begin{figure}
    \centering
    \includegraphics[width=0.88\textwidth]{attention_comparison}
    \caption{Matrizes de peso de atenção em uma sequência de 40 eventos.
             RoTHP: atenção dispersa sem padrão de decaimento.
             HoTHP: atenção concentrada próxima à diagonal, decaindo
             para eventos distantes.}
  \end{figure}
\end{frame}
```

- [ ] **Step 2: Compile and verify**

```bash
cd overleaf-apresentacao && pdflatex apresentacao.tex
```
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add overleaf-apresentacao/apresentacao.tex
git commit -m "presentation: add Block 5 (RoPE e RoTHP)"
```

---

## Task 9: Block 6 — HoPE e HoTHP (slides 22–27)

**Files:**
- Modify: `overleaf-apresentacao/apresentacao.tex`

- [ ] **Step 1: Append Block 6 before `\end{document}`**

```latex
% ============================================================================
\section{HoTHP}
% ============================================================================

% ── Slide 22: HoPE ───────────────────────────────────────────────────────────
\begin{frame}{HoPE: codificação hiperbólica para LLMs}
  Dai et al. \cite{dai2025hope} substituíram o kernel trigonométrico do
  RoPE por funções hiperbólicas com um parâmetro de decaimento treinável
  $\theta'$:
  \begin{itemize}
    \item O kernel hiperbólico \textbf{decai} com a distância entre posições.
    \item O parâmetro $\theta'$ é aprendido durante o treinamento e
          restringido a $\theta' > \max(\theta_j)$, garantindo decaimento.
    \item Resultado: perplexidade estável em sequências mais longas que as
          vistas no treinamento, em modelos de linguagem.
  \end{itemize}
  \begin{block}{Motivação para PTTs}
    O decaimento exponencial do HoPE é exatamente o comportamento esperado
    em um Processo de Hawkes.
  \end{block}
\end{frame}

% ── Slide 23: o kernel hiperbólico ───────────────────────────────────────────
\begin{frame}{O kernel hiperbólico}
  Em vez de rotacionar $\mathbf{q}$ e $\mathbf{k}$, o HoTHP calcula o
  score diretamente como função de $\Delta t_{ij} = t_i - t_j$:
  \[
    \text{score}(i,j) = \frac{1}{\sqrt{d_k}}
      \sum_{\ell=1}^{d_k/2}
      \bigl[
        (q_{2\ell} k_{2\ell} + q_{2\ell+1} k_{2\ell+1}) \cdot
          e^{-|\delta|\theta'} \cosh(\delta\theta_\ell)
        + (q_{2\ell} k_{2\ell+1} + q_{2\ell+1} k_{2\ell}) \cdot
          e^{-|\delta|\theta'} \sinh(\delta\theta_\ell)
      \bigr]
  \]
  onde $\delta = \Delta t_{ij}$ e $\theta_\ell = 10000^{-2(\ell-1)/d}$.

  O fator $e^{-|\delta|\theta'}$ garante que scores entre eventos
  distantes se aproximem de zero.
\end{frame}

% ── Slide 24: por que hiperbólico funciona para Hawkes? ──────────────────────
\begin{frame}{Alinhamento com o Processo de Hawkes}
  O decaimento embutido no kernel hiperbólico reproduz a estrutura
  da função de excitação $\varphi(\tau) = \alpha e^{-\beta \tau}$:
  \begin{itemize}
    \item Eventos recentes recebem maior peso de atenção.
    \item Eventos distantes têm influência próxima de zero.
    \item O parâmetro $\theta'$ é aprendido e representa a taxa de
          decaimento global do processo.
  \end{itemize}
  \begin{block}{Diferença fundamental em relação ao RoTHP}
    O RoTHP codifica posição relativa sem viés de recência. O HoTHP
    codifica posição relativa \textbf{e} viés de decaimento em um único
    mecanismo.
  \end{block}
\end{frame}

% ── Slide 25: arquitetura ────────────────────────────────────────────────────
\begin{frame}{Arquitetura do HoTHP}
  \begin{figure}
    \centering
    \includegraphics[width=0.80\textwidth]{architecture}
    \caption{Arquitetura completa do HoTHP. O bloco de atenção hiperbólica
             substitui a atenção padrão do THP e a atenção rotacional do
             RoTHP.}
  \end{figure}
\end{frame}

% ── Slide 26: detalhe de implementação ───────────────────────────────────────
\begin{frame}{Detalhes de implementação}
  \textbf{Normalização causal de timestamps}
  \begin{itemize}
    \item Os timestamps são normalizados de forma causal (usando apenas
          os eventos anteriores), de modo que o gap médio seja
          aproximadamente 1.0.
    \item Isso evita que a escala absoluta dos timestamps influencie os
          scores.
  \end{itemize}
  \vspace{0.4em}
  \textbf{Estabilidade numérica}
  \begin{itemize}
    \item $\cosh(x)$ cresce como $e^x$ e pode causar overflow para
          $\Delta t$ grande.
    \item Solução: distribuir $e^{-|\delta|\theta'}$ para dentro do
          $\cosh$, reduzindo o cálculo a somas de exponenciais com
          expoentes negativos.
  \end{itemize}
\end{frame}

% ── Slide 27: tabela resumo ───────────────────────────────────────────────────
\begin{frame}{Comparativo: THP, RoTHP e HoTHP}
  \begin{table}
    \centering
    \begin{tabular}{lcccc}
      \toprule
      \textbf{Propriedade} & \textbf{THP} & \textbf{RoTHP} & \textbf{HoTHP} \\
      \midrule
      Invariante temporal        & Não  & Sim  & Sim \\
      Viés de decaimento         & Não  & Não  & Sim (aprendido) \\
      Extrapola para seq. longas & Não  & Parcial & Sim \\
      Custo posicional           & $O(Ld)$ & $O(Ld)$ & $O(L^2 d/2)$ \\
      Parâmetros extras          & 0    & 0    & 1 ($\theta'$) \\
      \bottomrule
    \end{tabular}
    \caption{Comparativo entre THP, RoTHP e HoTHP.}
  \end{table}
\end{frame}
```

- [ ] **Step 2: Compile and verify**

```bash
cd overleaf-apresentacao && pdflatex apresentacao.tex
```
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add overleaf-apresentacao/apresentacao.tex
git commit -m "presentation: add Block 6 (HoPE e HoTHP)"
```

---

## Task 10: Block 7 — Experimentos (slides 28–34)

**Files:**
- Modify: `overleaf-apresentacao/apresentacao.tex`

The NLL values below come from `overleaf-bracis/figures/gen_main_result.py`.

- [ ] **Step 1: Append Block 7 before `\end{document}`**

```latex
% ============================================================================
\section{Experimentos}
% ============================================================================

% ── Slide 28: setup experimental ────────────────────────────────────────────
\begin{frame}{Configuração experimental}
  \begin{itemize}
    \item \textbf{Dados:} sequências sintéticas geradas por Processos de
          Hawkes com dois regimes de decaimento:
          \begin{itemize}
            \item Decaimento lento ($\beta \approx 0{,}025$): influência
                  dos eventos persiste por longo período.
            \item Decaimento rápido ($\beta \approx 0{,}40$): influência
                  decai rapidamente.
          \end{itemize}
    \item \textbf{Protocolo:} treinamento em sequências de comprimento
          $N_{\text{train}}$; teste em $\alpha \cdot N_{\text{train}}$,
          com $\alpha \in \{1, 2, 5, 10\}$.
    \item \textbf{Modelos avaliados:} THP, RoTHP e HoTHP sob condições
          idênticas de treinamento.
    \item \textbf{Métrica:} NLL médio sobre as sequências de teste.
  \end{itemize}
\end{frame}

% ── Slide 29: perfil de atenção ───────────────────────────────────────────────
\begin{frame}{Perfil de atenção teórico}
  \begin{figure}
    \centering
    \includegraphics[width=0.82\textwidth]{attention_profile}
    \caption{Score de atenção em função de $\Delta t$.
             RoTHP oscila sem decaimento. HoTHP decai monotonicamente,
             alinhado com o comportamento teórico do Processo de Hawkes.}
  \end{figure}
\end{frame}

% ── Slide 30: resultado principal ────────────────────────────────────────────
\begin{frame}{Resultado principal}
  \begin{figure}
    \centering
    \includegraphics[width=0.88\textwidth]{main_result}
    \caption{NLL em função do fator de extrapolação $\alpha$.
             (a) Decaimento lento: modelos têm desempenho similar.
             (b) Decaimento rápido: HoTHP mantém NLL estável;
             RoTHP degrada progressivamente.}
  \end{figure}
\end{frame}

% ── Slide 31: alinhamento do kernel ──────────────────────────────────────────
\begin{frame}{Alinhamento do kernel aprendido}
  \begin{figure}
    \centering
    \includegraphics[width=0.82\textwidth]{kernel_alignment}
    \caption{Comparação entre o kernel aprendido pelo HoTHP e o kernel
             teórico do Processo de Hawkes. O HoTHP aprende uma forma
             funcional próxima ao decaimento exponencial teórico.}
  \end{figure}
\end{frame}

% ── Slide 32: tabela de resultados numéricos ─────────────────────────────────
\begin{frame}{Resultados numéricos}
  \begin{table}
    \centering
    \small
    \begin{tabular}{llcccc}
      \toprule
      \textbf{Dataset} & \textbf{Modelo} & $\alpha=1$ & $\alpha=2$ & $\alpha=5$ & $\alpha=10$ \\
      \midrule
      \multirow{2}{*}{Decaimento lento}
        & RoTHP  & $0{,}937$ & $1{,}005$ & $1{,}389$ & $1{,}540$ \\
        & HoTHP  & $0{,}950$ & $1{,}031$ & $1{,}403$ & $1{,}545$ \\
      \midrule
      \multirow{2}{*}{Decaimento rápido}
        & RoTHP  & $0{,}965$ & $1{,}583$ & $2{,}036$ & $2{,}362$ \\
        & HoTHP  & $0{,}958$ & $1{,}170$ & $1{,}484$ & $1{,}593$ \\
      \bottomrule
    \end{tabular}
    \caption{NLL médio (média $\pm$ desvio padrão omitido; ver figura anterior)
             por modelo e fator de extrapolação.}
  \end{table}
\end{frame}

% ── Slide 33: custo computacional ────────────────────────────────────────────
\begin{frame}{Custo computacional}
  \begin{table}
    \centering
    \begin{tabular}{lccc}
      \toprule
      \textbf{Modelo} & \textbf{Onde incide o custo posicional} & \textbf{Custo} & \textbf{Tempo rel.} \\
      \midrule
      THP    & Encoding aditivo por evento  & $O(Ld)$      & $1\times$ \\
      RoTHP  & Rotação de $\mathbf{q}$, $\mathbf{k}$ por evento & $O(Ld)$ & $1\times$ \\
      HoTHP  & Kernel calculado por \textbf{par} de eventos & $O(L^2 d/2)$ & $3\times$ \\
      \bottomrule
    \end{tabular}
    \caption{Custo do mecanismo posicional e tempo de treinamento relativo
             em condições iguais.}
  \end{table}
  \vspace{0.3em}
  O HoTHP treina $3\times$ mais lento que o RoTHP porque o kernel
  hiperbólico precisa ser calculado para todos os $L^2$ pares de eventos,
  enquanto RoPE opera por evento individualmente.
\end{frame}

% ── Slide 34: análise de break-even ──────────────────────────────────────────
\begin{frame}{Quando a extrapolação compensa?}
  Se treinar em sequências $\alpha$ vezes mais longas custa $\alpha^2$ vezes
  mais (custo quadrático da atenção), o HoTHP se paga quando:
  \[
    \alpha^2 > 3 \implies \alpha > \sqrt{3} \approx 1{,}73
  \]
  Ou seja: para sequências de teste mais que $73\%$ maiores que as de
  treino, treinar o HoTHP em sequências curtas já é mais barato do que
  treinar o RoTHP diretamente em sequências longas.
  \begin{alertblock}{Limitação}
    Este argumento é teórico. O experimento empírico que mede o tempo de
    treinamento sem extrapolação em função de $L$ ainda não foi realizado
    e está listado como trabalho futuro.
  \end{alertblock}
\end{frame}
```

- [ ] **Step 2: Compile and verify**

```bash
cd overleaf-apresentacao && pdflatex apresentacao.tex
```
Expected: no errors. Verify the numerical table uses `\multirow` correctly (requires `\usepackage{multirow}` in preamble — add it if not present).

- [ ] **Step 3: Add `multirow` to preamble if missing**

In `apresentacao.tex`, ensure the preamble contains:
```latex
\usepackage{multirow}
```

- [ ] **Step 4: Commit**

```bash
git add overleaf-apresentacao/apresentacao.tex
git commit -m "presentation: add Block 7 (Experimentos)"
```

---

## Task 11: Block 8 — Conclusão (slides 35–37)

**Files:**
- Modify: `overleaf-apresentacao/apresentacao.tex`

- [ ] **Step 1: Append Block 8 and bibliography before `\end{document}`**

```latex
% ============================================================================
\section{Conclusão}
% ============================================================================

% ── Slide 35: contribuições ──────────────────────────────────────────────────
\begin{frame}{Contribuições}
  \begin{itemize}
    \item \textbf{HoTHP:} primeiro modelo de PTT com codificação de posição
          hiperbólica, combinando invariância temporal e viés de decaimento
          em um único mecanismo de atenção.
    \item O parâmetro treinável $\theta'$ aprende a taxa de decaimento
          global do processo, alinhando o modelo com a estrutura teórica
          do Processo de Hawkes.
    \item Avaliação no protocolo \emph{train short, test long} mostra NLL
          estável para fatores de extrapolação até $10\times$, enquanto
          o RoTHP degrada no regime de decaimento rápido.
    \item Código disponível no repositório da dissertação (EasyTPP).
  \end{itemize}
\end{frame}

% ── Slide 36: limitações e trabalhos futuros ─────────────────────────────────
\begin{frame}{Limitações e trabalhos futuros}
  \textbf{Limitações atuais}
  \begin{itemize}
    \item Avaliação restrita a dados sintéticos; desempenho em datasets
          reais ainda não foi verificado.
    \item Custo computacional $3\times$ maior que o RoTHP pode ser
          proibitivo para sequências muito longas.
    \item O experimento de break-even empírico não foi realizado.
  \end{itemize}
  \vspace{0.4em}
  \textbf{Trabalhos futuros}
  \begin{itemize}
    \item Avaliar em datasets reais (MIMIC, Stack Overflow, Retweet).
    \item Medir o crossover empírico entre custo de extrapolação e custo
          de treino longo.
    \item Investigar aproximações de baixo posto para reduzir o custo
          $O(L^2)$ do kernel hiperbólico.
    \item Análise teórica da convergência e das propriedades do $\theta'$
          aprendido.
  \end{itemize}
\end{frame}

% ── Slide 37: encerramento ────────────────────────────────────────────────────
\begin{frame}{Obrigado}
  \begin{center}
    \large
    \textbf{Hugo Ramos Soares} \\
    \normalsize
    \texttt{hugoramos84@gmail.com} \\[1em]
    Universidade Federal do Ceará \\
    Orientador: César Lincoln C. Mattos \\[1.5em]
    \textbf{Referências principais}
  \end{center}
  \begin{itemize}
    \item Hawkes (1971) — Processo de Hawkes original
    \item Zuo et al. (2020) — THP
    \item Su et al. (2024) — RoFormer/RoPE
    \item Dai et al. (2026) — RoTHP
    \item Dai et al. (2025) — HoPE
    \item Press et al. (2022) — Train Short, Test Long
  \end{itemize}
\end{frame}

% ── Bibliografia completa (slide oculto) ────────────────────────────────────
\begin{frame}[allowframebreaks]{Referências}
  \bibliographystyle{plain}
  \bibliography{refs}
\end{frame}

\end{document}
```

- [ ] **Step 2: Compile twice (bibliography needs two passes)**

```bash
cd overleaf-apresentacao && pdflatex apresentacao.tex && bibtex apresentacao && pdflatex apresentacao.tex && pdflatex apresentacao.tex
```
Expected: `apresentacao.pdf` with all 37 slides, bibliography resolved, no errors.

- [ ] **Step 3: Commit**

```bash
git add overleaf-apresentacao/apresentacao.tex
git commit -m "presentation: add Block 8 (Conclusao) and complete 37-slide deck"
```

---

## Task 12: Break-even experiment

**Files:**
- Create: `notebooks/breakeven_experiment.py`

This script trains THP, RoTHP, and HoTHP on synthetic Hawkes sequences of increasing lengths *without extrapolation* and records wall-clock time per epoch. The results will close the cost-benefit argument in the presentation.

- [ ] **Step 1: Create the experiment script**

```python
# notebooks/breakeven_experiment.py
"""
Break-even experiment: wall-clock training time vs sequence length.

Trains THP, RoTHP, and HoTHP on synthetic Hawkes sequences of lengths
[64, 128, 256, 512] (no extrapolation) and records seconds-per-epoch.

Results answer: for what sequence length does training HoTHP on short
sequences become cheaper than training RoTHP on the full long sequence?

Usage:
    python3.9 notebooks/breakeven_experiment.py
"""
import time
import json
import torch
import numpy as np

# ── synthetic data generator ──────────────────────────────────────────────────
def simulate_hawkes(mu, alpha, beta, T, max_events=2000, seed=0):
    """Simulate a univariate Hawkes process via Ogata thinning."""
    rng = np.random.default_rng(seed)
    events = []
    t = 0.0
    while t < T and len(events) < max_events:
        lam_bar = mu + alpha * sum(np.exp(-beta * (t - s)) for s in events)
        w = rng.exponential(1.0 / lam_bar)
        t_cand = t + w
        if t_cand > T:
            break
        lam_t = mu + alpha * sum(np.exp(-beta * (t_cand - s)) for s in events)
        if rng.uniform() < lam_t / lam_bar:
            events.append(t_cand)
        t = t_cand
    return np.array(events)


def make_batch(seq_len, batch_size=8, beta=0.4, seed=0):
    """Return (time_seqs, type_seqs, masks) tensors of shape [B, L]."""
    seqs = []
    for i in range(batch_size):
        ev = simulate_hawkes(mu=0.2, alpha=0.5, beta=beta,
                             T=seq_len * 5, max_events=seq_len + 1, seed=seed + i)
        ev = ev[:seq_len]
        if len(ev) < seq_len:
            ev = np.pad(ev, (0, seq_len - len(ev)), constant_values=ev[-1] if len(ev) else 0.0)
        seqs.append(ev)
    times = torch.tensor(np.array(seqs), dtype=torch.float32)
    types = torch.zeros(batch_size, seq_len, dtype=torch.long)
    mask  = torch.zeros(batch_size, seq_len, seq_len, dtype=torch.bool)
    for b in range(batch_size):
        for i in range(seq_len):
            mask[b, i, i+1:] = True  # causal: future events masked
    return times, types, mask


# ── timing harness ────────────────────────────────────────────────────────────
def time_forward(model_cls, model_config_cls, seq_len, n_repeats=20):
    """Return mean seconds per forward pass for given model and seq_len."""
    import sys, os
    sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..'))

    cfg = model_config_cls(
        hidden_size=64,
        time_emb_size=16,
        num_layers=2,
        num_heads=4,
        dropout_rate=0.1,
        use_ln=False,
        num_event_types=1,
        device='cpu',
    )
    model = model_cls(cfg)
    model.train()

    times_batch, types_batch, mask_batch = make_batch(seq_len)

    # Warmup
    for _ in range(3):
        model(times_batch, types_batch, mask_batch)

    torch.manual_seed(0)
    t0 = time.perf_counter()
    for _ in range(n_repeats):
        model(times_batch, types_batch, mask_batch)
    elapsed = time.perf_counter() - t0
    return elapsed / n_repeats


def main():
    from easy_tpp.model.torch_model.torch_thp import THP
    from easy_tpp.model.torch_model.torch_rothp import RoTHP
    from easy_tpp.model.torch_model.torch_hothp import HoTHP
    from easy_tpp.config_factory.model_config import ModelConfig

    seq_lengths = [64, 128, 256, 512]
    results = {}

    for name, cls in [('THP', THP), ('RoTHP', RoTHP), ('HoTHP', HoTHP)]:
        results[name] = {}
        for L in seq_lengths:
            t = time_forward(cls, ModelConfig, L)
            results[name][L] = round(t, 4)
            print(f"{name:8s}  L={L:4d}  {t*1000:.1f} ms/fwd")

    out = 'notebooks/breakeven_results.json'
    with open(out, 'w') as f:
        json.dump(results, f, indent=2)
    print(f"\nResults saved to {out}")

    # Print ratio table
    print("\nHoTHP / RoTHP time ratio:")
    for L in seq_lengths:
        ratio = results['HoTHP'][L] / results['RoTHP'][L]
        print(f"  L={L}: {ratio:.2f}x")


if __name__ == '__main__':
    main()
```

- [ ] **Step 2: Run the experiment**

```bash
cd /Users/hugoramossoares/Sites/myprojects/ufc/dissertacao
python3.9 notebooks/breakeven_experiment.py
```
Expected: prints a table of ms/forward-pass per model and sequence length, saves `notebooks/breakeven_results.json`.

- [ ] **Step 3: Update slide 34 with empirical results**

Open `overleaf-apresentacao/apresentacao.tex`. In slide 34 (break-even slide), replace the `\alertblock` content with the empirical ratio from `breakeven_results.json` if it confirms or adjusts the $3\times$ figure. Update the `\alertblock` status from "limitação" to "resultado confirmado" accordingly.

- [ ] **Step 4: Recompile and commit**

```bash
cd overleaf-apresentacao && pdflatex apresentacao.tex && bibtex apresentacao && pdflatex apresentacao.tex
git add notebooks/breakeven_experiment.py notebooks/breakeven_results.json overleaf-apresentacao/apresentacao.tex
git commit -m "presentation: add break-even experiment and update slide 34 with empirical results"
```

---

## Self-Review

**Spec coverage check:**

| Spec requirement | Task |
|---|---|
| Madrid theme, Portuguese, no em-dashes | Task 1 |
| Block 1: motivation + cost curve | Tasks 1, 3, 4 |
| Block 2: TPP, Hawkes, NLL, comparative table | Task 5 |
| Block 3: NHP, Transformer, THP, limitation | Task 6 |
| Block 4: length extrapolation, Press et al., formal definition | Task 7 |
| Block 5: RoPE, RoTHP, limitation, new attention comparison figure | Tasks 2, 8 |
| Block 6: HoPE, HoTHP, architecture, summary table | Task 9 |
| Block 7: setup, 3 figures, numerical table, cost table, break-even | Task 10 |
| Block 8: contributions, limitations, future work | Task 11 |
| Break-even experiment | Task 12 |

All spec requirements are covered. No placeholders found. Type names are consistent across all tasks (`THP`, `RoTHP`, `HoTHP`, `ModelConfig`). The `\multirow` package is required and noted in Task 10 Step 3.
